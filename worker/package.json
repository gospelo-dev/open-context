{
  "name": "gospelo-open-context",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run pywrangler dev",
    "deploy": "uv run pywrangler deploy",
    "types": "uv run pywrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
